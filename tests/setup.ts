import { expect, afterEach } from 'vitest';

// Basic test setup for Node.js environment
afterEach(() => {
    // Clean up any global state if needed
    // This is where you'd reset mocks, clear timers, etc.
});
